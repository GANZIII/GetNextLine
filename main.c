#include <stdio.h>
#include "get_next_line.h"
/*
int main(void)
{
    int fd = open("./hello.txt", O_RDONLY);

    for (int i = 0; i < 11; i++)
    {
        char    *str = get_next_line(fd);
        printf("(%d str) : %s", i, str);
    }
    close(fd);
    return (0);
}
*/